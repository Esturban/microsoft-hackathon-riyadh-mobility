# Power BI Assets

This folder is reserved for optional Power BI artifacts.

Suggested contents if you extend the repo:

- exported `.pbix` files
- KPI screenshots for judges
- a short note describing the dataset fields used by the dashboard
