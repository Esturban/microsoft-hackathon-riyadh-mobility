# Agent Build Notes

- Built thin vertical slice first: sample data -> API -> map/dashboard -> Azure scaffold.
- Kept frontend in vanilla JS and backend in FastAPI per spec.
- Azure integrations are optional at runtime and degrade to sample mode.
