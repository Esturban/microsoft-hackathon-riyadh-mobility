# Azure Deployment

1. Install Azure CLI and Azure Developer CLI.
2. `az login`
3. `azd up`
4. Set Container App image if you want a custom registry flow.
5. Run the data scripts to upload processed files and seed Cosmos DB.
