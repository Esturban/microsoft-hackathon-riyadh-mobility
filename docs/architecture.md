# Architecture

Local mode:

`browser -> FastAPI -> static frontend + sample GeoJSON/JSON`

Cloud MVP mode:

`browser -> Container App -> FastAPI/static frontend -> Blob Storage + Cosmos DB -> Azure Maps`
